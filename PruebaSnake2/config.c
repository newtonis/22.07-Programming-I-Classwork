#include "config.h"

/// In this file there are the configuration things that need values to be stored to them

double diff_array[CNT_DIFF] = { // difficulty levels speeds, in seconds
    0.120,
    0.100,
    0.070,
    0.050,
    0.020,
};